# DreamList
Eine schlanke, lokale ToDo-App als Desktop-Anwendung (Electron + React + TypeScript + Vite). Aufgaben und Listen werden ausschliesslich lokal in einer JSON-Datei gespeichert — keine Cloud, keine Anmeldung.
